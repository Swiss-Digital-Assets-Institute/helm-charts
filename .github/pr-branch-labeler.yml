# Apply label "bugfix" if head matches one of "bugfix/*" or "hotfix/*"
patch:
  head: ["fix/*", "bugfix/*", "hotfix/*", "feature/*"]

# Apply label "feature" if head matches "feature/*"
minor:
  head: ["release/*"]

# Apply label "feature" if head matches "feature/*"
major:
  head: ["breaking/*", "major/*", "BREAKING/*"]
